<div class="row justify-content-center">
  <div class="col-md-5">
    <p>boo!boo!bookは読書習慣を助けるアプリです。たくさん読書をしてぶたさんを育ててみましょう！</p>
    <%=  form_with model: @first_registration, url: {controller: 'first_registrations', action: 'create' } do |f| %>
      <%= f.hidden_field :name, value: @pork.name %>
      <%= f.hidden_field :character_type, value: 0 %>
      <%= f.hidden_field :level, value: 1 %>
      <%= f.hidden_field :exp, value: 0 %>
      <%= f.hidden_field :description, value: @pork.description %>
      <%= f.hidden_field :photo, value: @pork.photo %>
      <%= f.hidden_field :user_id, value: current_user.id %>

      <div class="row">
        <p>書籍を登録</p>
        <div class="form-group py-3">
          <%= f.label :book_name, "書籍名" %>
          <%= f.text_field :book_name, class: "form-control", placeholder: "必須" %>
        </div>
        <div class="form-group py-3">
          <%= f.label :isbn, "isbnコード" %>
          <%= f.text_field :isbn, class: "form-control", placeholder: "数字のみ（任意）" %>
          <%= f.hidden_field :status, value: "読書中" %>
        </div>
        <div class="form-group py-3">
          <%= f.label :memo, "メモ" %>
          <%= f.text_field :memo, class: "form-control", placeholder: "任意"  %>
        </div>
        <%= f.hidden_field :user_id, value: current_user.id %>
        <div class="form-group py-3 d-grid gap-1 col-4 mx-auto">
          <%= f.submit "さあはじめよう！", class: "btn btn-pink" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
